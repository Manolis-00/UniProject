spring:
  #mvc
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  # Security
  security:
    user:
      name: manolis
      password: 12345
  # Datasource
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password: password
    url: jdbc:h2:file:./data/bankDatabase
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
      path: /h2-console